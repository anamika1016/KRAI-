default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password: postgres
  host: localhost

development:
  <<: *default
  database: vrp_development

test:
  <<: *default
  database: vrp_test

production:
  <<: *default
  database: vrp_production